{
  "name": "browser-control",
  "version": "1.1.2",
  "description": "Default browser automation for Canopy Code: navigate, click, persistently select dropdown options, snapshot, cookies, storage, and authenticated fetch via a real browser over CDP/BiDi.",
  "mcpServers": {
    "browser-control": {
      "command": "${extensionPath}${/}scripts${/}mcp-wrapper.sh"
    }
  }
}
